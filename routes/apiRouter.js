const express = require('express');
const router = express.Router();
let userController = require('../routes/apiRouter');


//router.get('/users', userController.list);
//router.get('/users/:id', usersController.user);


module.exports = router;